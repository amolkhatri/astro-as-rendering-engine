---
import { IslandPageAnimation } from '../components/q/IslandPageAnimation';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Island Architecture Demo</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <style>
      /* Import styles from slides page */
      :root {
        /* Modern Dark Theme */
        --bg-primary: #0f172a;
        --bg-secondary: #1e293b;
        --bg-terminal: #020617;
        --bg-code: #1e293b;
        
        /* Vibrant Accent */
        --accent: #818cf8;
        --accent-dim: rgba(129, 140, 248, 0.15);
        --accent-glow: 0 0 20px rgba(129, 140, 248, 0.3);
        
        /* Text */
        --text-primary: #f8fafc;
        --text-secondary: #cbd5e1;
        --text-dim: #94a3b8;
        --text-muted: #64748b;
        --border: #334155;
        
        --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
        --font-sans: 'Inter', system-ui, sans-serif;
      }
      
      * { box-sizing: border-box; margin: 0; padding: 0; }
      
      body {
        font-family: var(--font-sans);
        background: var(--bg-primary);
        color: var(--text-primary);
        padding: 2rem;
        min-height: 100vh;
      }

      h1 {
        color: var(--accent);
        text-shadow: var(--accent-glow);
        margin-bottom: 1rem;
        font-size: 2rem;
      }

      p {
        color: var(--text-secondary);
        margin-bottom: 2rem;
        line-height: 1.6;
      }

      /* Island Page Animation Styles */
      .island-page-anim {
        background: var(--bg-terminal);
        border: 1px solid var(--border);
        border-radius: 4px;
        padding: 1.5rem;
        margin: 2rem 0;
      }

      .ipa-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px dashed var(--border);
        flex-wrap: wrap;
        gap: 1rem;
      }

      .ipa-title {
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .ipa-icon { font-size: 1.5rem; }

      .ipa-label {
        font-size: 1rem;
        color: var(--accent);
        letter-spacing: 0.1em;
        font-weight: 600;
      }

      .ipa-controls { display: flex; gap: 0.5rem; }

      .ipa-toggle {
        background: var(--bg-code);
        border: 1px solid var(--border);
        color: var(--text-secondary);
        padding: 0.5rem 1rem;
        font-family: var(--font-mono);
        font-size: 0.85rem;
        cursor: pointer;
        transition: all 0.15s;
        border-radius: 4px;
      }

      .ipa-toggle:hover {
        border-color: var(--accent);
        color: var(--accent);
      }

      .ipa-layout {
        display: grid;
        grid-template-columns: 1fr 280px;
        gap: 1.5rem;
      }

      @media (max-width: 900px) {
        .ipa-layout { grid-template-columns: 1fr; }
      }

      /* Browser Mock */
      .ipa-browser {
        background: var(--bg-code);
        border: 1px solid var(--border);
        border-radius: 8px;
        overflow: hidden;
      }

      .browser-chrome {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 0.75rem 1rem;
        background: var(--bg-terminal);
        border-bottom: 1px solid var(--border);
      }

      .browser-dots { display: flex; gap: 6px; }
      .browser-dots .dot { width: 12px; height: 12px; border-radius: 50%; }
      .browser-dots .dot.red { background: #ff5f56; }
      .browser-dots .dot.yellow { background: #ffbd2e; }
      .browser-dots .dot.green { background: #27ca40; }

      .browser-url {
        flex: 1;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        background: var(--bg-code);
        padding: 0.4rem 0.75rem;
        border-radius: 4px;
        font-size: 0.8rem;
        color: var(--text-secondary);
      }

      .browser-viewport {
        padding: 1rem;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        max-height: 500px;
        overflow-y: auto;
      }

      /* Page Sections */
      .page-section {
        position: relative;
        border-radius: 6px;
        padding: 0.75rem;
        transition: all 0.2s ease;
        cursor: default;
      }

      .page-section.hovered { transform: translateX(4px); }

      .static-section {
        background: rgba(148, 163, 184, 0.15);
        border: 1px solid rgba(148, 163, 184, 0.3);
      }

      .static-section.hovered {
        border-color: rgba(148, 163, 184, 0.6);
        background: rgba(148, 163, 184, 0.2);
      }

      .dynamic-section {
        background: rgba(129, 140, 248, 0.15);
        border: 1px solid rgba(129, 140, 248, 0.3);
      }

      .dynamic-section.hovered {
        border-color: rgba(129, 140, 248, 0.6);
        background: rgba(129, 140, 248, 0.25);
      }

      .dynamic-section.loaded {
        border-color: rgba(129, 140, 248, 0.6);
        box-shadow: 0 0 15px rgba(129, 140, 248, 0.2);
      }

      .island-section {
        background: rgba(14, 165, 233, 0.08);
        border: 2px dashed rgba(14, 165, 233, 0.3);
        cursor: pointer;
      }

      .island-section.hovered {
        border-color: rgba(14, 165, 233, 0.6);
        background: rgba(14, 165, 233, 0.12);
      }

      .island-section.hydrated {
        border-style: solid;
        border-color: var(--accent);
        background: rgba(14, 165, 233, 0.2);
        box-shadow: 0 0 20px rgba(14, 165, 233, 0.2);
      }

      /* Section Badges */
      .section-badge {
        position: absolute;
        top: 0.5rem;
        right: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.3rem;
        padding: 0.2rem 0.5rem;
        border-radius: 3px;
        font-size: 0.65rem;
        font-weight: 600;
        letter-spacing: 0.05em;
      }

      .static-badge { background: rgba(148, 163, 184, 0.3); color: #94a3b8; }
      .dynamic-badge { background: rgba(129, 140, 248, 0.3); color: #818cf8; }
      .island-badge { background: var(--accent-dim); color: var(--accent); }
      .badge-icon { font-size: 0.8rem; }

      /* Island Hints & Indicators */
      .island-hint {
        position: absolute;
        bottom: 0.5rem;
        right: 0.5rem;
        font-size: 0.7rem;
        color: var(--text-dim);
        animation: pulse-hint 1.5s ease infinite;
      }

      @keyframes pulse-hint {
        0%, 100% { opacity: 0.6; }
        50% { opacity: 1; }
      }

      .hydrated-indicator {
        position: absolute;
        bottom: 0.5rem;
        right: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.3rem;
        padding: 0.25rem 0.5rem;
        background: var(--accent-dim);
        border: 1px solid var(--accent);
        border-radius: 3px;
        font-size: 0.7rem;
        color: var(--accent);
        animation: glow-indicator 2s ease infinite;
      }

      @keyframes glow-indicator {
        0%, 100% { box-shadow: 0 0 5px rgba(14, 165, 233, 0.3); }
        50% { box-shadow: 0 0 15px rgba(14, 165, 233, 0.5); }
      }

      /* Mock Content Styles */
      .section-content { font-size: 0.8rem; }
      
      .nav-mock { display: flex; justify-content: space-between; align-items: center; }
      .nav-logo { font-weight: 600; color: var(--text-secondary); }
      .nav-links { display: flex; gap: 1rem; color: var(--text-dim); font-size: 0.75rem; }

      .hero-mock { padding: 0.5rem 0; }
      .hero-greeting { font-size: 1rem; color: var(--text-secondary); margin-bottom: 0.25rem; }
      .user-name { color: #818cf8; font-weight: 600; }
      .hero-subtitle { font-size: 0.8rem; color: var(--text-dim); margin-bottom: 0.5rem; }
      .hero-stats { display: flex; gap: 1rem; }
      .hero-stats .stat { font-size: 0.75rem; color: var(--text-secondary); padding: 0.25rem 0.5rem; background: rgba(129, 140, 248, 0.2); border-radius: 3px; }

      .content-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.5rem; }
      .content-card { background: var(--bg-terminal); border-radius: 4px; padding: 0.5rem; }
      .card-image { height: 40px; background: linear-gradient(135deg, var(--bg-code) 0%, var(--border) 100%); border-radius: 3px; margin-bottom: 0.35rem; }
      .card-title { font-size: 0.7rem; color: var(--text-secondary); margin-bottom: 0.2rem; }
      .card-desc { font-size: 0.6rem; color: var(--text-dim); }

      .form-mock { padding: 0.25rem 0; }
      .form-title { font-size: 0.85rem; color: var(--accent); margin-bottom: 0.5rem; font-weight: 600; }
      .form-field { margin-bottom: 0.4rem; }
      .field-label { display: block; font-size: 0.65rem; color: var(--text-dim); margin-bottom: 0.15rem; }
      .field-input { height: 24px; background: var(--bg-terminal); border: 1px solid var(--border); border-radius: 3px; }
      .field-textarea { height: 40px; background: var(--bg-terminal); border: 1px solid var(--border); border-radius: 3px; }
      .form-button { display: inline-block; padding: 0.35rem 0.75rem; background: var(--bg-terminal); border: 1px solid var(--accent); color: var(--accent); font-size: 0.75rem; border-radius: 3px; margin-top: 0.35rem; }
      .island-section.hydrated .form-button { background: var(--accent); color: var(--bg-terminal); font-weight: 600; }

      .carousel-mock { display: flex; align-items: center; gap: 0.5rem; position: relative; padding: 0.5rem 0; }
      .carousel-arrow { width: 24px; height: 24px; display: flex; align-items: center; justify-content: center; background: var(--bg-terminal); border: 1px solid var(--border); border-radius: 3px; color: var(--text-dim); font-size: 1rem; }
      .island-section.hydrated .carousel-arrow { border-color: var(--accent); color: var(--accent); }
      .carousel-slides { flex: 1; display: flex; gap: 0.35rem; overflow: hidden; }
      .carousel-slide { flex: 0 0 60%; height: 60px; background: linear-gradient(135deg, var(--bg-code) 0%, var(--border) 100%); border-radius: 4px; opacity: 0.5; }
      .carousel-slide.active { opacity: 1; border: 1px solid var(--border); }
      .island-section.hydrated .carousel-slide.active { border-color: var(--accent); }
      .carousel-dots { position: absolute; bottom: 0; left: 50%; transform: translateX(-50%); display: flex; gap: 4px; }
      .carousel-dot { width: 6px; height: 6px; background: var(--border); border-radius: 50%; }
      .carousel-dot.active { background: var(--text-dim); }
      .island-section.hydrated .carousel-dot.active { background: var(--accent); }

      .footer-mock { display: flex; justify-content: space-between; align-items: center; }
      .footer-links { display: flex; gap: 1rem; color: var(--text-dim); font-size: 0.7rem; }
      .footer-copyright { font-size: 0.7rem; color: var(--text-muted); }

      /* Info Panel */
      .ipa-info-panel { display: flex; flex-direction: column; gap: 0.75rem; }
      .info-section { background: var(--bg-code); border: 1px solid var(--border); border-radius: 6px; overflow: hidden; }
      .info-header { display: flex; align-items: center; gap: 0.5rem; padding: 0.6rem 0.75rem; font-size: 0.75rem; font-weight: 600; letter-spacing: 0.05em; }
      .static-header { background: rgba(148, 163, 184, 0.2); color: #94a3b8; }
      .dynamic-header { background: rgba(129, 140, 248, 0.2); color: #818cf8; }
      .island-header { background: var(--accent-dim); color: var(--accent); }
      .info-icon { font-size: 1rem; }
      .info-content { padding: 0.75rem; }
      .info-content p { margin: 0 0 0.5rem; font-size: 0.8rem; color: var(--text-secondary); }
      .info-content ul { margin: 0; padding-left: 0; list-style: none; }
      .info-content li { font-size: 0.75rem; color: var(--text-dim); padding: 0.2rem 0; }

      /* Summary */
      .ipa-summary { display: flex; align-items: center; justify-content: center; gap: 1rem; margin-top: 1.5rem; padding: 1rem; background: var(--bg-code); border: 1px solid var(--border); border-radius: 6px; flex-wrap: wrap; }
      .summary-item { display: flex; align-items: center; gap: 0.5rem; }
      .summary-label { font-size: 0.85rem; color: var(--text-dim); }
      .summary-value { font-size: 0.9rem; font-weight: 600; }
      .static-value { color: #94a3b8; }
      .island-value { color: var(--accent); }
      .js-zero { color: var(--accent); text-shadow: var(--accent-glow); }
      .js-loaded { color: var(--accent); }
      .server-loading { color: #818cf8; animation: pulse-text 1s ease infinite; }
      .server-loaded { color: #818cf8; font-weight: 600; }
      .summary-divider { color: var(--border); }

      @keyframes pulse-text { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }

      /* Insight */
      .ipa-insight { display: flex; align-items: flex-start; gap: 0.75rem; margin-top: 1rem; padding: 1rem; background: linear-gradient(135deg, var(--accent-dim) 0%, rgba(14, 165, 233, 0.05) 100%); border: 1px solid var(--accent); border-radius: 6px; }
      .insight-icon { font-size: 1.5rem; flex-shrink: 0; }
      .insight-text { font-size: 0.9rem; color: var(--text-secondary); line-height: 1.6; }
      .insight-text strong { color: var(--accent); }
      .insight-text .highlight { color: var(--accent); font-weight: 600; text-shadow: var(--accent-glow); }

      /* Skeleton Loading Styles */
      .skeleton-container { position: relative; }
      .skeleton-line { height: 14px; background: linear-gradient(90deg, rgba(129, 140, 248, 0.2) 25%, rgba(129, 140, 248, 0.4) 50%, rgba(129, 140, 248, 0.2) 75%); background-size: 200% 100%; animation: skeleton-shimmer 1.5s ease-in-out infinite; border-radius: 4px; }
      .skeleton-line.short { width: 60%; }
      .skeleton-line.medium { width: 80%; height: 12px; margin-top: 0.5rem; }

      @keyframes skeleton-shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }

      .skeleton-greeting { margin-bottom: 0.25rem; }
      .skeleton-subtitle { margin-bottom: 0.5rem; }
      .skeleton-stats { display: flex; gap: 0.75rem; margin-bottom: 0.5rem; }
      .skeleton-stat { width: 80px; height: 24px; background: linear-gradient(90deg, rgba(129, 140, 248, 0.15) 25%, rgba(129, 140, 248, 0.3) 50%, rgba(129, 140, 248, 0.15) 75%); background-size: 200% 100%; animation: skeleton-shimmer 1.5s ease-in-out infinite; border-radius: 4px; }

      .loading-indicator { display: flex; align-items: center; gap: 0.5rem; margin-top: 0.5rem; font-size: 0.7rem; color: #818cf8; }
      .loading-spinner { width: 12px; height: 12px; border: 2px solid rgba(129, 140, 248, 0.3); border-top-color: #818cf8; border-radius: 50%; animation: spin 1s linear infinite; }
      .loading-text { opacity: 0.8; }
      @keyframes spin { to { transform: rotate(360deg); } }

      .loaded-content { animation: fadeInContent 0.5s ease; }
      @keyframes fadeInContent { from { opacity: 0; transform: translateY(-5px); } to { opacity: 1; transform: translateY(0); } }

      .server-rendered-badge { display: flex; align-items: center; gap: 0.35rem; margin-top: 0.5rem; padding: 0.3rem 0.5rem; background: rgba(129, 140, 248, 0.2); border: 1px solid rgba(129, 140, 248, 0.4); border-radius: 4px; font-size: 0.7rem; color: #818cf8; width: fit-content; }
      .server-icon { font-size: 0.85rem; }

      /* Recommendations Section */
      .recs-title { font-size: 0.8rem; color: #818cf8; margin-bottom: 0.5rem; font-weight: 600; }
      .recs-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.5rem; }
      .rec-card { background: var(--bg-terminal); border-radius: 4px; padding: 0.5rem; text-align: center; }
      .rec-card.skeleton { padding: 0.5rem; }
      .skeleton-image { height: 30px; background: linear-gradient(90deg, rgba(129, 140, 248, 0.15) 25%, rgba(129, 140, 248, 0.3) 50%, rgba(129, 140, 248, 0.15) 75%); background-size: 200% 100%; animation: skeleton-shimmer 1.5s ease-in-out infinite; border-radius: 4px; margin-bottom: 0.35rem; }
      .rec-card.skeleton .skeleton-line { height: 10px; width: 70%; margin: 0 auto; }
      .rec-image { font-size: 1.25rem; margin-bottom: 0.25rem; }
      .rec-title { font-size: 0.65rem; color: var(--text-secondary); }
    </style>
  </head>
  <body>
    <h1>ISLAND ARCHITECTURE DEMO</h1>
    <p>Watch how static content appears instantly, personalized content streams from the server (with skeleton loading), and interactive islands hydrate only when you click them.</p>
    <IslandPageAnimation client:visible />
  </body>
</html>
