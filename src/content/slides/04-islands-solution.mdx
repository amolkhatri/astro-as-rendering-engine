---
title: "islands --arch"
order: 4
icon: "🏝️"
---

import { IslandPageAnimation } from '../../components/q/IslandPageAnimation';

1. Less or no javascript
2. Faster Server Response

# ISLAND ARCHITECTURE
## The Web's New Mental Model

Think of a web page as an **ocean of static HTML** with small **interactive islands** floating in it.
Most of the page doesn't need JavaScript — only the parts that need to respond to user actions.

<IslandPageAnimation client:visible />

## The Three Types of Content

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  📄 STATIC CONTENT                                              │
│  ├── Headers, footers, navigation                              │
│  ├── Text, images, layout                                      │
│  └── Requires: ZERO JavaScript                                  │
│      → Rendered as pure HTML, instantly visible                 │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  👤 DYNAMIC / PERSONALIZED                                      │
│  ├── "Welcome back, Sarah!"                                    │
│  ├── User-specific recommendations                             │
│  └── Requires: Server rendering, but NO client JavaScript      │
│      → Data fetched on server, sent as HTML                     │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🏝️ INTERACTIVE ISLANDS                                         │
│  ├── Forms, carousels, modals                                  │
│  ├── Any component that responds to clicks/input               │
│  └── Requires: JavaScript, but ONLY when user interacts        │
│      → Zero JS until needed, then loads ~5-15kb per island      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## How Hydration Works

| Content Type | Initial Load | On Interaction |
|-------------|--------------|----------------|
| **Static** | HTML only (0kb JS) | — No change |
| **Dynamic** | HTML only (0kb JS) | — No change |
| **Interactive** | HTML only (0kb JS) | → Loads island JS (~5-15kb) |

## The Key Insight

> 🎯 **Most of your page doesn't need JavaScript!**
>
> - Navigation? Pure HTML links work fine
> - Content? Already rendered on server  
> - Images? Just `<img>` tags
> - Footer? Static HTML
>
> Only buttons, forms, and interactive widgets need JavaScript.
> And they only need it **when the user actually interacts**.

## Traditional SPA vs Islands

```
TRADITIONAL SPA:
└── Load EVERYTHING upfront: 2-5MB JavaScript
    └── Parse all components
    └── Hydrate entire page
    └── User waits 3-8 seconds

ISLANDS ARCHITECTURE:
└── Load NOTHING upfront: 0kb JavaScript  
    └── Page visible instantly
    └── User clicks form → Load 8kb
    └── User clicks carousel → Load 5kb
    └── Total if all used: ~13kb (vs 2-5MB!)
```

## Why This Matters

| Metric | Traditional | Islands |
|--------|------------|---------|
| Initial JS | 2-5MB | 0kb |
| Time to Visible | 3-8 seconds | < 100ms |
| Time to Interactive | Same as visible | Instant for static |
| JS per interaction | Already loaded | ~5-15kb on-demand |

*This is the foundation of Astro's performance →*
